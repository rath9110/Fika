Contacts are the core entity -never silently delete or modify them
Always handle the offline/unauthenticated state (localStorage)
Sync conflicts: local data takes precedence until user is authenticated